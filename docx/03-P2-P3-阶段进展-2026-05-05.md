# P2/P3 阶段进展（2026-05-05）

## 本次完成
- P2.1 异步导入任务化：
  - 新增 `knowledge_import_task` 模型与建表脚本。
  - `AddKnowledgeFile` 改为“创建任务后异步执行”，状态推进改为 `pending/processing/done/failed`。
  - 幂等策略：处理中重复请求直接返回；已结束可重入触发新任务。
- P2.2 真删闭环：
  - 实现 `RemoveKnowledgeFile` 服务与 HTTP 处理。
  - 删除闭环清理：`knowledge_file`、`knowledge_document_chunk`、`knowledge_import_task`。
  - 删除后重建 namespace 索引，避免检索残留。
- P2.3 失败分类与阶段日志：
  - 增加分类：`PARAM_ERROR / PERMISSION_ERROR / BUSINESS_ERROR / SYSTEM_ERROR`。
  - 导入过程分阶段日志：`knowledge.import.start/parsing/chunking/embedding/indexing/done/failed`。
  - 同步写结构化日志 + `agent_action_step`。
- P3 演示体验：
  - 知识库详情页状态自动轮询（2.5s）。
  - 知识库文件列表状态彩色标签、支持“移除”。
  - 对话区增加 RAG 命中依据折叠区（source/score/snippet）。

## 变更文件
- 后端：
  - `internal/agent/model/dbmodel/knowledge_import_task.go`
  - `internal/agent/module/module.go`
  - `internal/agent/service/knowledge_file_service.go`
  - `internal/agent/api/handler.go`
  - `deploy/mysql/init.sql`
- 前端：
  - `frontend/src/api/storage.js`
  - `frontend/src/hooks/useCloudDriveController.jsx`
  - `frontend/src/components/files/FilesPanel.jsx`
  - `frontend/src/App.jsx`

## 验证
- `go test ./...` 通过。
- `cd frontend && npm run build` 通过。

## 待你实测关注
- 导入后状态推进是否按 parsing -> chunking -> embedding -> indexing -> done。
- 删除知识库文件后，RAG 问同样问题是否不再命中旧 chunk。
- 导入失败时，错误是否有分类并可通过日志定位。
