# P1-P4 执行记录（2026-04-30）

## P1 核心工具接入
- 完成 `tool.file.list`：复用 file service，支持关键词检索。
- 完成 `tool.share.list`：复用 share service，返回分享列表。
- 完成 `tool.share.records`：复用 share service，支持按 shareId/默认最近分享查询访问记录。
- 输出统一结构：`items/summary/sources/traceId/toolResults`。

## P2 权限与可观测性
- Agent 查询强制登录态校验，并绑定 `principal.workspaceId`。
- 新增 workspace 一致性校验：请求 workspace 与登录 workspace 不一致直接拒绝。
- 新增审计日志表 `agent_audit_log`（启动自动建表），记录 traceId、工具、耗时、状态、输入输出快照。
- 工具调用增加超时控制（2s）与部分降级（partial=true）。

## P3 联调与面试化
- 新增后端接口：`POST /apis/agent/query`。
- 新增前端一级导航：`Agent 检索`。
- 前端新增只读检索面板：输入 query、展示 summary/traceId/sources/结果表。
- 前端调用自动透传 `X-Workspace-Id`、`X-Storage-Setting-Id`，与后端权限链路闭环。

## P4 验证结果（本轮）
- `go build -o /tmp/mcd-api ./cmd/api`：通过。
- `go test ./...`：失败（已有测试 `test/file_api_integration_test.go` 使用旧签名 mock，非本次 Agent 改动引入）。
- `frontend npm run build`：通过。
- `frontend npm test -- --runInBand`：失败（vitest 不支持该参数，脚本问题）。

## 典型问题模板映射（10 条）
1. 最近上传了哪些文件
2. 按关键词查询文件 xxx
3. 根目录有哪些文件
4. 我的分享有哪些
5. 哪些分享允许下载
6. 最近谁访问了我的分享
7. 某个分享 shr_xxx 的访问记录
8. 近期开启的分享有哪些
9. 最近更新的文件有哪些
10. 当前空间下文件与分享概览

## 已知风险
- 复杂自然语言还未引入 LLM，仅规则映射 intent。
- 测试目录存在既有用例与当前 service 构造签名不一致。
